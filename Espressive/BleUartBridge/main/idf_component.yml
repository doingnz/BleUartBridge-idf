dependencies:
  # WS2812 RGB LED driver used on ESP32-S3 DevKit (GPIO48).
  # Also compiled (but unused) on ESP32 so the same CMakeLists works for both targets.
  espressif/led_strip:
    version: ">=2.0.0"
  idf:
    version: ">=5.0.0"
